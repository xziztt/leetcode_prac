#include<bits/stdc++.h>
#include<ctype.h>

using namespace std;
vector<string> spellchecker(vector<string>& wordlist, vector<string>& queries) {
  unordered_set<string> uset_wordlist;
  unordered_map<string,string> umap_lowercase;
  for(int i = 0;i < wordlist.size();i++){
    std::transform(wordlist[i].begin())
  }
  cout<<wordlist[0];
}


int main(){
    std::vector<string> wordlist = {"HeLLo"};
    std::vector<string> queries = {"hEllO"};
    spellchecker(wordlist,queries);

}
