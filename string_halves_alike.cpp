class Solution {
public:
    bool halvesAreAlike(string s) {
    	std::unordered_set<char> uset = {a,e,i,o,u,A,E,I,O,U};
    	int halfIndex = s.length() - 1;
    	for(int i = 0;i < halfIndex;i++){
    		if(uset[s[i]])
    	}
        
        
        
    }
};